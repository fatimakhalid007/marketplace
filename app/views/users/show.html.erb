<% provide(:title, 'Profile') %>
<h1>User Profile</h1>

<div class="row">
  <div class="span6 offset3">
     <%= form_for(@user, html:{multipart:true}) do |f| %>
     <%= render 'fields1', f: f %>
    <% end %>
  </div>
</div>
