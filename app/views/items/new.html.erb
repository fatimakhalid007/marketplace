<% provide(:title, 'Add Items') %>
<h1>Add Item</h1>

<div class="row">
  <div class="span6 offset3">
     <%= form_for(@item, html:{multipart:true}) do |f| %>
     <%= render 'itemAdd', f: f %>

      <%= f.submit "Add Item", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>